// A sintaaxe básica do eperador ternário é;
//"Condição" ? "retorno se verdadeira" : "Retorno se falsa";

let temperature = 30;
let weather = temperature > 30 ? "Tá quente" : "Tá de boa";

//let weather = temperature > 30 && "Tá quente" //apenas se for verdadeira

console.log(` A temperatura é ${temperature}, portando, ${weather}`);
